# ACC — Agent Command Center

A local command center for agent tasks, live activity, project changes, and review history on Windows and Linux.

The implementation and agreed project plan will arrive through a tracked pull request from `temporary` into `main`.
